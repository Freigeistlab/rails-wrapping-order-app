<%= form_with(model: product_group, local: true) do |form| %>
  <% if product_group.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(product_group.errors.count, "error") %> prohibited this product_group from being saved:</h2>

      <ul>
      <% product_group.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :order %>
    <%= form.number_field :order %>
  </div>

  <div class="field">
    <%= form.label :active %>
    <%= form.check_box :active %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
