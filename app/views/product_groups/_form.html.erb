<%= simple_form_for @product_group do |f| %>
  <%= f.error_notification %>

  <%= f.input :name %>

  <%= f.input :order %>

  <%= f.input :active %>

  <%= f.button :submit, class: "btn-primary" %>
<% end %>
